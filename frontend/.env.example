# Base URL of the EverAfter AI FastAPI backend (include /api/v1).
NEXT_PUBLIC_API_BASE_URL=http://localhost:8000/api/v1

# Optional — enables the "Continue with Google" button on login/register.
# Leave blank to hide the button entirely.
NEXT_PUBLIC_GOOGLE_CLIENT_ID=
